--- a/index.html
+++ b/index.html
@@ -14,6 +14,13 @@
           </label>
           <span class="theme-label">Dark Mode</span>
         </div>
+        <div class="theme-switch-wrapper">
+          <label class="theme-switch" for="countdownToggle">
+            <input type="checkbox" id="countdownToggle" />
+            <span class="slider round"></span>
+          </label>
+          <span class="theme-label">ETA Countdown</span>
+        </div>
       </div>
 
       <div class="controls">
@@ -49,12 +56,18 @@
       const statusMessagesDiv = document.getElementById("status-messages");
       const stopSelectionAreaDiv = document.getElementById("stop-selection-area");
       const etaResultsAreaDiv = document.getElementById("eta-results-area");
+      const countdownToggle = document.getElementById('countdownToggle');
       const darkModeToggle = document.getElementById('darkModeToggle');
       
       let stopNameCache = {};
+      let activeEtaCells = []; // To store cells that need countdown updates
+      let countdownUpdateInterval = null;
+      let currentSearchContext = null; // To store details for auto-refresh
+      let autoRefreshTimer = null;
 
       function saveToCache(key, data) {
         const cacheEntry = { timestamp: new Date().getTime(), data: data };
         localStorage.setItem(key, JSON.stringify(cacheEntry));
       }
 
@@ -65,6 +78,17 @@
         catch (e) { localStorage.removeItem(key); return null; }
       }
 
+      function saveSetting(key, value) {
+        localStorage.setItem(key, JSON.stringify(value));
+      }
+
+      function loadSetting(key, defaultValue) {
+        const item = localStorage.getItem(key);
+        if (item === null) return defaultValue;
+        try { return JSON.parse(item); }
+        catch (e) { localStorage.removeItem(key); return defaultValue; }
+      }
+
       function isCacheValid(cacheEntry, expiryHours = CACHE_EXPIRY_HOURS) {
         if (!cacheEntry || !cacheEntry.timestamp) return false;
         const now = new Date().getTime();
@@ -154,6 +178,23 @@
         catch (e) { return "-"; }
       }
 
+      function formatEtaCountdown(isoTimestamp) {
+        if (!isoTimestamp) return "-";
+        const etaTime = new Date(isoTimestamp).getTime();
+        const now = new Date().getTime();
+        let diff = Math.round((etaTime - now) / 1000)); // Difference in seconds
+
+        if (diff <= 0) { // Show "Due" for 0 or negative
+            const scheduledBusCell = document.querySelector(`td[data-eta-timestamp="${isoTimestamp}"].scheduled-eta`);
+            if (scheduledBusCell) return formatEtaTime(isoTimestamp); // Show original time if scheduled and past
+            return diff < -15 ? "Departed" : "Due"; // Show "Departed" if more than 15s past, else "Due"
+        }
+
+        const minutes = Math.floor(diff / 60);
+        const seconds = diff % 60;
+
+        if (minutes > 0) return `${minutes}m ${seconds}s`;
+        return `${seconds}s`;
+      }
+
       function formatRouteNumberForDisplay(routeStr) {
         if (!routeStr) return '-';
         return routeStr;
@@ -169,8 +210,7 @@
         return { prefix: prefix, mainNumStr: mainNumStr, mainNumLen: mainNumStr.length, suffix: suffix, original: routeStr };
       }
 
-      function renderEtaTable(stopIdentifier, etasFromApi, isGrouped = false, platformForTitle = null) {
+      function renderEtaTable(stopIdentifier, etasFromApi, isGrouped = false, platformForTitle = null, showCountdown = false) {
         console.log(`Rendering ETA table for: ${isGrouped ? stopIdentifier.groupName : stopIdentifier.name_en}, isGrouped: ${isGrouped}, platformForTitle: ${platformForTitle}`);
         const container = document.createElement("div");
         container.className = "eta-table-container";
@@ -178,6 +218,7 @@
         if (isGrouped) title.textContent = `ETAs for ${stopIdentifier.groupName} ${platformForTitle ? `- Platform: ${platformForTitle}` : ''}`;
         else title.textContent = `ETAs for ${stopIdentifier.name_en} / ${stopIdentifier.name_tc}`;
         container.appendChild(title);
+        container.dataset.stopIdentifier = JSON.stringify(stopIdentifier); // Store for refresh
 
         if (!etasFromApi || etasFromApi.length === 0) {
           const p = document.createElement("p");
@@ -246,16 +287,23 @@
                 for (let i = 0; i < 3; i++) {
                     const td = document.createElement("td");
                     if (timedEtas[i]) {
-                        let etaDisplay = formatEtaTime(timedEtas[i].eta);
+                        const isScheduled = timedEtas[i].rmk_en === "Scheduled Bus" || timedEtas[i].rmk_tc === "原定班次";
+                        td.dataset.etaTimestamp = timedEtas[i].eta; // Store for countdown/toggle
+                        
+                        let etaDisplayContent = showCountdown && !isScheduled ? formatEtaCountdown(timedEtas[i].eta) : formatEtaTime(timedEtas[i].eta);
                         let remarkText = timedEtas[i].rmk_en || timedEtas[i].rmk_tc || "";
-                        if (timedEtas[i].rmk_en === "Scheduled Bus" || timedEtas[i].rmk_tc === "原定班次") td.classList.add("scheduled-eta");
-                        else if (remarkText) {
+                        
+                        let displayHtml = `<span class="eta-time-value">${etaDisplayContent}</span>`;
+
+                        if (isScheduled) td.classList.add("scheduled-eta");
+                        
+                        if (remarkText && !isScheduled) { // Avoid double remark for scheduled
                             const symbol = remarkSymbols[remarkSymbolIndex % remarkSymbols.length];
-                            etaDisplay += `<span class="remark-symbol">${symbol}</span>`;
+                            displayHtml += `<span class="remark-symbol">${symbol}</span>`;
                             if (!allRemarksForDisplay.has(remarkText)) { allRemarksForDisplay.set(remarkText, symbol); remarkSymbolIndex++; }
                         }
-                        td.innerHTML = etaDisplay;
+                        td.innerHTML = displayHtml;
+                        if (showCountdown && !isScheduled && timedEtas[i].eta) activeEtaCells.push(td);
                     } else td.textContent = "-";
                     row.appendChild(td);
                 }
@@ -300,9 +348,30 @@
         tbody.appendChild(fragment);
         container.appendChild(table);
         etaResultsAreaDiv.appendChild(container);
-      }
-      
+      }
+
+      function updateDisplayedEtaFormat() {
+        const showCountdown = countdownToggle.checked;
+        document.querySelectorAll('td[data-eta-timestamp]').forEach(cell => {
+            const timestamp = cell.dataset.etaTimestamp;
+            const isScheduled = cell.classList.contains("scheduled-eta");
+            const timeValueSpan = cell.querySelector('.eta-time-value');
+
+            if (timestamp && timeValueSpan) {
+                timeValueSpan.textContent = showCountdown && !isScheduled ? formatEtaCountdown(timestamp) : formatEtaTime(timestamp);
+                if (showCountdown && !isScheduled) {
+                    if (!activeEtaCells.includes(cell)) activeEtaCells.push(cell);
+                } else {
+                    activeEtaCells = activeEtaCells.filter(c => c !== cell);
+                }
+            }
+        });
+
+        if (showCountdown && activeEtaCells.length > 0) startCountdownUpdater();
+        else stopCountdownUpdater();
+      }
+
       async function handleSearch() {
         console.log("handleSearch triggered.");
         const stopQuery = stopNameInput.value.trim();
@@ -315,7 +384,11 @@
             return; 
         }
         etaResultsAreaDiv.innerHTML = ""; stopSelectionAreaDiv.innerHTML = ""; 
+        activeEtaCells = []; // Clear for new search
+        stopCountdownUpdater(); // Stop any existing countdowns
+        if (autoRefreshTimer) clearInterval(autoRefreshTimer); // Clear existing refresh timer
         console.log("Setting initial status to 'Searching...'");
+        currentSearchContext = { stopQuery, routeFilters, useSmartGrouping, type: null, data: null }; // Reset context
         setStatus("Searching...", "loading");
 
         try {
@@ -342,6 +415,7 @@
           
           if (useSmartGrouping) {
             console.log("Smart grouping enabled.");
+            currentSearchContext.type = 'smartGroup';
             const groupedByBaseName = {};
             matchedStops.forEach(stop => {
                 const { baseName } = parsePlatformFromStopName(stop.name_en);
@@ -349,6 +423,7 @@
                 groupedByBaseName[baseName].stops.push(stop);
             });
 
+            currentSearchContext.data = groupedByBaseName;
             for (const groupKey in groupedByBaseName) {
                 const group = groupedByBaseName[groupKey];
                 let allEtasForGroup = [];
@@ -369,11 +444,12 @@
                 
                 console.log(`ETAs fetched for group ${group.groupName}. Count: ${allEtasForGroup.length}`);
                 if (allEtasForGroup.length > 0 || group.stops.length > 0) {
-                     renderEtaTable({ groupName: group.groupName }, allEtasForGroup, true, null);
+                     renderEtaTable({ groupName: group.groupName, stops: group.stops }, allEtasForGroup, true, null, countdownToggle.checked);
                 }
             }
           } else {
             console.log("Smart grouping disabled. Fetching for individual matched stops.");
+            currentSearchContext.type = 'individual';
+            currentSearchContext.data = matchedStops;
             for (const stop of matchedStops) {
                 console.log(`Fetching ETAs for individual stop: ${stop.name_en}`);
                 setStatus(`Fetching ETAs for: ${stop.name_en}...`, "loading");
@@ -381,7 +457,7 @@
                 console.log(`ETAs fetched for stop ${stop.name_en}. Count: ${etasFromApi.length}`);
                 if (routeFilters.length > 0) etasFromApi = etasFromApi.filter(eta => routeFilters.includes(eta.route.toUpperCase()));
                 renderEtaTable(stop, etasFromApi, false); // isGrouped is false
-            }
+            } // The renderEtaTable call for individual stops needs the countdownToggle.checked
           }
           
           if (etaResultsAreaDiv.innerHTML === "") {
@@ -391,49 +467,83 @@
             console.log("ETA results rendered, clearing final status.");
             clearStatus();
           }
+          if (countdownToggle.checked && activeEtaCells.length > 0) startCountdownUpdater();
+          // Start auto-refresh timer if there are results
+          if (etaResultsAreaDiv.innerHTML !== "" && !autoRefreshTimer) {
+            autoRefreshTimer = setInterval(refreshCurrentEtas, 60000);
+          }
 
         } catch (error) {
           console.error("Error in handleSearch:", error);
           setStatus(`Search Error: ${error.message}. Check console.`, "error");
         }
       }
+
+      async function refreshCurrentEtas() {
+        if (!currentSearchContext || !currentSearchContext.type || etaResultsAreaDiv.innerHTML === "") {
+            console.log("Refresh: No current search context or no results to refresh.");
+            if (autoRefreshTimer) { clearInterval(autoRefreshTimer); autoRefreshTimer = null; }
+            return;
+        }
+        console.log("Auto-refreshing ETAs...");
+        // Optionally: setStatus("Refreshing ETAs...", "loading", true); // true for silent update
+
+        const tempActiveEtaCells = []; // Use a temporary array for new cells
+
+        try {
+            const { routeFilters, useSmartGrouping, type, data } = currentSearchContext;
+            etaResultsAreaDiv.innerHTML = ""; // Clear existing tables before re-rendering
+
+            if (type === 'smartGroup' && data) {
+                for (const groupKey in data) {
+                    const group = data[groupKey];
+                    let allEtasForGroup = [];
+                    const etaPromises = group.stops.map(stop => getStopEta(stop.stop).catch(() => []));
+                    const etaResultsArray = await Promise.all(etaPromises);
+                    etaResultsArray.forEach(etas => allEtasForGroup.push(...etas));
+                    if (routeFilters.length > 0) {
+                        allEtasForGroup = allEtasForGroup.filter(eta => routeFilters.includes(eta.route.toUpperCase()));
+                    }
+                    // Pass current countdown state to renderEtaTable
+                    renderEtaTable({ groupName: group.groupName, stops: group.stops }, allEtasForGroup, true, null, countdownToggle.checked);
+                }
+            } else if (type === 'individual' && data) {
+                for (const stop of data) { // data is matchedStops array
+                    let etasFromApi = await getStopEta(stop.stop);
+                    if (routeFilters.length > 0) etasFromApi = etasFromApi.filter(eta => routeFilters.includes(eta.route.toUpperCase()));
+                    renderEtaTable(stop, etasFromApi, false, null, countdownToggle.checked);
+                }
+            }
+            // activeEtaCells will be repopulated by renderEtaTable if countdowns are on
+            if (countdownToggle.checked && activeEtaCells.length > 0) {
+                 startCountdownUpdater();
+            } else {
+                 stopCountdownUpdater();
+            }
+            // if (statusMessagesDiv.textContent === "Refreshing ETAs...") clearStatus();
+        } catch (error) {
+            console.error("Error during auto-refresh:", error);
+            // setStatus("Error refreshing ETAs.", "error");
+        }
+      }
+
+      function startCountdownUpdater() {
+        stopCountdownUpdater(); // Clear any existing interval
+        if (activeEtaCells.length > 0 && countdownToggle.checked) {
+          countdownUpdateInterval = setInterval(() => {
+            activeEtaCells = activeEtaCells.filter(cell => {
+              if (!document.body.contains(cell)) return false; // Cell removed from DOM
+              const timestamp = cell.dataset.etaTimestamp;
+              const timeValueSpan = cell.querySelector('.eta-time-value');
+              if (timestamp && timeValueSpan && !cell.classList.contains('scheduled-eta')) {
+                timeValueSpan.textContent = formatEtaCountdown(timestamp);
+              }
+              return true; // Keep cell in list unless explicitly removed
+            });
+            if (activeEtaCells.length === 0) stopCountdownUpdater();
+          }, 1000);
+        }
+      }
+
+      function stopCountdownUpdater() {
+        if (countdownUpdateInterval) clearInterval(countdownUpdateInterval);
+        countdownUpdateInterval = null;
+      }
 
       searchButton.addEventListener("click", handleSearch);
       stopNameInput.addEventListener("keypress", (event) => { if (event.key === "Enter") handleSearch(); });
       routeFilterInput.addEventListener("keypress", (event) => { if (event.key === "Enter") handleSearch(); });
-
-      function setDarkMode(isDark) {
-        if (isDark) { document.body.classList.add('dark-mode'); localStorage.setItem('theme', 'dark'); }
-        else { document.body.classList.remove('dark-mode'); localStorage.setItem('theme', 'light'); }
-        darkModeToggle.checked = isDark;
-      }
-      darkModeToggle.addEventListener('change', (event) => { setDarkMode(event.target.checked); });
-      const savedTheme = localStorage.getItem('theme');
-      if (savedTheme) { setDarkMode(savedTheme === 'dark'); }
-      else { const prefersDark = window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches; setDarkMode(prefersDark); }
+      countdownToggle.addEventListener('change', (event) => { setCountdownMode(event.target.checked); });
+      darkModeToggle.addEventListener('change', (event) => { setDarkMode(event.target.checked); });
+
+      function setDarkMode(isDark) {
+        document.body.classList.toggle('dark-mode', isDark);
+        saveSetting('theme', isDark ? 'dark' : 'light');
+        darkModeToggle.checked = isDark;
+      }
+
+      function setCountdownMode(showCountdown) {
+        saveSetting('showCountdown', showCountdown);
+        countdownToggle.checked = showCountdown;
+        updateDisplayedEtaFormat(); // Update existing ETAs immediately
+      }
+
+      // Initial setup
+      const savedTheme = loadSetting('theme', (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) ? 'dark' : 'light');
+      setDarkMode(savedTheme === 'dark');
+
+      const savedCountdownPreference = loadSetting('showCountdown', false); // Default to false
+      setCountdownMode(savedCountdownPreference);
+      // Ensure toggle reflects loaded setting if updateDisplayedEtaFormat doesn't set it
+      countdownToggle.checked = savedCountdownPreference; 
 
       setStatus("Enter a stop name to begin.", "info");
     </script>
--- a/singyin.html
+++ b/singyin.html
@@ -10,6 +10,13 @@
     <div class="container">
       <div class="header-controls">
         <h1>Pak Hung House - Bus ETA</h1>
+        <div class="theme-switch-wrapper">
+          <label class="theme-switch" for="countdownToggleSy">
+            <input type="checkbox" id="countdownToggleSy" />
+            <span class="slider round"></span>
+          </label>
+          <span class="theme-label">ETA Countdown</span>
+        </div>
       </div>
 
       <div id="status-messages"></div>
@@ -29,13 +36,26 @@
       const API_BASE_URL = "https://data.etabus.gov.hk/v1/transport/kmb";
       const STOP_CACHE_KEY = "kmb_stop_data_cache_singyin"; 
       const CACHE_EXPIRY_HOURS = 23;
-      const ETA_REFRESH_INTERVAL = 60000;
+      const ETA_REFRESH_INTERVAL = 60000; // 1 minute
 
       const statusMessagesDiv = document.getElementById("status-messages");
       const etaResultsAreaDiv = document.getElementById("eta-results-area");
+      const countdownToggleSy = document.getElementById('countdownToggleSy');
       
       let fullStopDetailsCache = {}; 
       let etaRefreshTimer = null;
+      let activeEtaCellsSy = [];
+      let countdownUpdateIntervalSy = null;
+
+      function saveSettingSy(key, value) {
+        localStorage.setItem(key + '_sy', JSON.stringify(value));
+      }
+
+      function loadSettingSy(key, defaultValue) {
+        const item = localStorage.getItem(key + '_sy');
+        if (item === null) return defaultValue;
+        try { return JSON.parse(item); } catch (e) { localStorage.removeItem(key + '_sy'); return defaultValue; }
+      }
 
       const predefinedStopGroups = [
         {
@@ -140,7 +160,23 @@
         catch (e) { return "-"; }
       }
 
+      function formatEtaCountdownSy(isoTimestamp) {
+        if (!isoTimestamp) return "-";
+        const etaTime = new Date(isoTimestamp).getTime();
+        const now = new Date().getTime();
+        let diff = Math.round((etaTime - now) / 1000));
+
+        if (diff <= 0) {
+             const scheduledBusCell = document.querySelector(`td[data-eta-timestamp="${isoTimestamp}"].scheduled-eta`);
+            if (scheduledBusCell) return formatEtaTime(isoTimestamp);
+            return diff < -15 ? "Departed" : "Due";
+        }
+        const minutes = Math.floor(diff / 60);
+        const seconds = diff % 60;
+
+        if (minutes > 0) return `${minutes}m ${seconds}s`;
+        return `${seconds}s`;
+      }
+
       function formatRouteNumberForDisplay(routeStr) {
         if (!routeStr) return '-';
         return routeStr;
@@ -157,7 +193,7 @@
         return { prefix: prefix, mainNumStr: mainNumStr, mainNumLen: mainNumStr.length, suffix: suffix, original: routeStr };
       }
 
-      function renderEtaTable(groupDisplayName, etasFromApi, stopPlatformMap) {
+      function renderEtaTable(groupDisplayName, etasFromApi, stopPlatformMap, showCountdown = false) {
         console.log(`Rendering ETA table for (singyin): ${groupDisplayName}`);
         let groupContainer = document.getElementById(`group-container-${groupDisplayName.replace(/\s+/g, '-')}`);
         if (!groupContainer) {
@@ -236,16 +272,23 @@
                 for (let i = 0; i < 3; i++) {
                     const td = document.createElement("td");
                     if (timedEtas[i]) {
-                        let etaDisplay = formatEtaTime(timedEtas[i].eta);
+                        const isScheduled = timedEtas[i].rmk_en === "Scheduled Bus" || timedEtas[i].rmk_tc === "原定班次";
+                        td.dataset.etaTimestamp = timedEtas[i].eta;
+
+                        let etaDisplayContent = showCountdown && !isScheduled ? formatEtaCountdownSy(timedEtas[i].eta) : formatEtaTime(timedEtas[i].eta);
                         let remarkText = timedEtas[i].rmk_en || timedEtas[i].rmk_tc || "";
-                        if (timedEtas[i].rmk_en === "Scheduled Bus" || timedEtas[i].rmk_tc === "原定班次") td.classList.add("scheduled-eta");
-                        else if (remarkText) {
+
+                        let displayHtml = `<span class="eta-time-value">${etaDisplayContent}</span>`;
+
+                        if (isScheduled) td.classList.add("scheduled-eta");
+
+                        if (remarkText && !isScheduled) {
                             const symbol = remarkSymbols[remarkSymbolIndex % remarkSymbols.length];
-                            etaDisplay += `<span class="remark-symbol">${symbol}</span>`;
+                            displayHtml += `<span class="remark-symbol">${symbol}</span>`;
                             if (!allRemarksForDisplay.has(remarkText)) { allRemarksForDisplay.set(remarkText, symbol); remarkSymbolIndex++; }
                         }
-                        td.innerHTML = etaDisplay;
+                        td.innerHTML = displayHtml;
+                        if (showCountdown && !isScheduled && timedEtas[i].eta) activeEtaCellsSy.push(td);
                     } else td.textContent = "-";
                     row.appendChild(td);
                 }
@@ -289,12 +332,30 @@
         tbody.appendChild(fragment);
         tableContainer.appendChild(table);
         groupContainer.appendChild(tableContainer);
-      }
+      }
+
+      function updateDisplayedEtaFormatSy() {
+        const showCountdown = countdownToggleSy.checked;
+        document.querySelectorAll('#eta-results-area td[data-eta-timestamp]').forEach(cell => {
+            const timestamp = cell.dataset.etaTimestamp;
+            const isScheduled = cell.classList.contains("scheduled-eta");
+            const timeValueSpan = cell.querySelector('.eta-time-value');
+
+            if (timestamp && timeValueSpan) {
+                timeValueSpan.textContent = showCountdown && !isScheduled ? formatEtaCountdownSy(timestamp) : formatEtaTime(timestamp);
+                if (showCountdown && !isScheduled) {
+                    if (!activeEtaCellsSy.includes(cell)) activeEtaCellsSy.push(cell);
+                } else {
+                    activeEtaCellsSy = activeEtaCellsSy.filter(c => c !== cell);
+                }
+            }
+        });
+        if (showCountdown && activeEtaCellsSy.length > 0) startCountdownUpdaterSy();
+        else stopCountdownUpdaterSy();
+      }
       
       async function loadSingYinEtas(isUpdate = false) {
         console.log(isUpdate ? "Auto-refreshing ETAs..." : "Initial loadSingYinEtas triggered.");
-        if (!isUpdate) {
-            etaResultsAreaDiv.innerHTML = ""; 
+        if (!isUpdate) { 
             setStatus("Loading Pak Hung House ETAs...", "loading");
         } else {
             console.log("Background update in progress...");
@@ -302,10 +363,11 @@
 
         try {
             // await getFullStopList(); // Optional
-            if (!isUpdate) { // Only clear on initial full load, not on background refresh
+            if (!isUpdate) { 
                 etaResultsAreaDiv.innerHTML = ""; 
             }
-            stopCountdownUpdaterSy(); // Stop existing before re-render
-            activeEtaCellsSy = [];    // Clear before re-populating
+            stopCountdownUpdaterSy(); 
+            activeEtaCellsSy = [];    
+
             for (const group of predefinedStopGroups) {
                 let allEtasForGroup = [];
                 const stopPlatformMap = {}; 
@@ -328,7 +390,7 @@
                 
                 console.log(`ETAs fetched for group ${group.groupName}. Total raw ETAs: ${allEtasForGroup.length}`);
                 if (allEtasForGroup.length > 0 || group.stops.length > 0) {
-                    renderEtaTable(group.groupName, allEtasForGroup, stopPlatformMap, countdownToggleSy.checked);
+                    renderEtaTable(group.groupName, allEtasForGroup, stopPlatformMap, countdownToggleSy.checked);
                 }
             }
             
@@ -338,7 +400,7 @@
                 clearStatus();
             } else {
                 console.log("Silent update complete.");
-                if (countdownToggleSy.checked) startCountdownUpdaterSy();
+                if (countdownToggleSy.checked && activeEtaCellsSy.length > 0) startCountdownUpdaterSy(); else stopCountdownUpdaterSy();
             }
 
         } catch (error) {
@@ -347,29 +409,38 @@
         }
       }
 
+      function startCountdownUpdaterSy() {
+        stopCountdownUpdaterSy(); 
+        if (activeEtaCellsSy.length > 0 && countdownToggleSy.checked) {
+          countdownUpdateIntervalSy = setInterval(() => {
+            activeEtaCellsSy = activeEtaCellsSy.filter(cell => {
+              if (!document.body.contains(cell)) return false; 
+              const timestamp = cell.dataset.etaTimestamp;
+              const timeValueSpan = cell.querySelector('.eta-time-value');
+              if (timestamp && timeValueSpan && !cell.classList.contains('scheduled-eta')) {
+                timeValueSpan.textContent = formatEtaCountdownSy(timestamp);
+              }
+              return true;
+            });
+            if (activeEtaCellsSy.length === 0) stopCountdownUpdaterSy();
+          }, 1000);
+        }
+      }
+
+      function stopCountdownUpdaterSy() {
+        if (countdownUpdateIntervalSy) clearInterval(countdownUpdateIntervalSy);
+        countdownUpdateIntervalSy = null;
+      }
+
+      function setCountdownModeSy(showCountdown) {
+        saveSettingSy('showCountdownSy', showCountdown);
+        countdownToggleSy.checked = showCountdown;
+        updateDisplayedEtaFormatSy(); 
+      }
+
       document.addEventListener('DOMContentLoaded', () => {
-        loadSingYinEtas();
+        const savedCountdownPreferenceSy = loadSettingSy('showCountdownSy', false); // Default false
+        setCountdownModeSy(savedCountdownPreferenceSy);
+        countdownToggleSy.checked = savedCountdownPreferenceSy;
+
+        loadSingYinEtas(); // Initial load
         if (etaRefreshTimer) clearInterval(etaRefreshTimer);
         etaRefreshTimer = setInterval(() => loadSingYinEtas(true), ETA_REFRESH_INTERVAL);
-      });
+
+        countdownToggleSy.addEventListener('change', (event) => { setCountdownModeSy(event.target.checked); });
+      });
     </script>
 
     <footer>
